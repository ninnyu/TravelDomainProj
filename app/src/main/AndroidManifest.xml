<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.potatopaloozac.traveldomainproj">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ui.booking.businfo.BusInfoActivity"
            android:noHistory="true"
            android:theme="@style/NoAnimTheme" />
        <activity
            android:name=".ui.booking.seatinfo.SeatInfoActivity"
            android:noHistory="true"
            android:theme="@style/NoAnimTheme" />
        <activity
            android:name=".ui.coupon.CouponActivity"
            android:theme="@style/NoAnimTheme" />
        <activity
            android:name=".ui.gameschedule.GameScheduleActivity"
            android:theme="@style/NoAnimTheme" />
        <activity
            android:name=".ui.booking.BookingActivity"
            android:theme="@style/NoAnimTheme">
        </activity>
        <activity android:name=".BaseActivity" />
        <activity android:name=".ui.booking.transfer.TransferActivity" />
        <activity android:name=".ui.booking.payment.PaymentActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>-->

    </application>

</manifest>