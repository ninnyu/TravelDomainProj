<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.potatopaloozac.traveldomainproj">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"></activity>
        <activity android:name=".BaseActivity" />

        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.booking.BookingActivity">

        </activity>
        <activity android:name=".ui.booking.businfo.BusInfoActivity" />
        <activity android:name=".ui.booking.transfer.TransferActivity" />
        <activity android:name=".ui.booking.seatinfo.SeatInfoActivity" />
        <activity android:name=".ui.booking.payment.PaymentActivity"/>
        <activity android:name=".ui.coupon.CouponActivity" />
        <activity android:name=".ui.gameschedule.GameScheduleActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>